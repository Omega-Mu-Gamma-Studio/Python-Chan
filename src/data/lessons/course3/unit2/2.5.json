{
  "id": "2.5",
  "title": "Stacking and Blending",
  "type": "coding",
  "phase1": {
    "code": "# Stacking and Blending\n\nimport numpy as np\nimport matplotlib.pyplot as plt\nfrom sklearn.datasets import make_classification\nfrom sklearn.model_selection import train_test_split, cross_val_score\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.tree import DecisionTreeClassifier\nfrom sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier, StackingClassifier\nfrom sklearn.neighbors import KNeighborsClassifier\nfrom sklearn.svm import SVC\nfrom sklearn.metrics import accuracy_score\n\nprint(\"=== Stacking and Blending ===\\n\")\n\nprint(\"\"\"\nStacking combines multiple models using a meta-model.\n\nHow it works:\n  1. Train base models on training data\n  2. Base models make predictions on validation data\n  3. Use these predictions to train meta-model\n  4. Final prediction from meta-model\n\nBlending is similar but uses a hold-out set instead of cross-validation.\n\nPros:\n  ✅ Can improve performance\n  ✅ Leverages different models\n  ✅ Flexible\n\nCons:\n  ❌ More complex\n  ❌ Slower\n  ❌ Can overfit\n  ❌ Less interpretable\n\"\"\")\n\nprint(\"=== Creating Data ===\\n\")\n\nnp.random.seed(42)\nX, y = make_classification(n_samples=500, n_features=10, n_informative=8,\n                           n_redundant=2, random_state=42)\n\nX_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)\n\nprint(f\"Training: {len(X_train)}, Test: {len(X_test)}\")\n\nprint(\"\\n=== Individual Models ===\\n\")\n\n# Define base models\nbase_models = {\n    'Logistic Regression': LogisticRegression(max_iter=1000, random_state=42),\n    'Decision Tree': DecisionTreeClassifier(max_depth=5, random_state=42),\n    'Random Forest': RandomForestClassifier(n_estimators=50, max_depth=5, random_state=42),\n    'Gradient Boosting': GradientBoostingClassifier(n_estimators=50, max_depth=3, random_state=42),\n    'KNN': KNeighborsClassifier(n_neighbors=5)\n}\n\n# Evaluate individual models\nindividual_scores = {}\nfor name, model in base_models.items():\n    model.fit(X_train, y_train)\n    score = model.score(X_test, y_test)\n    individual_scores[name] = score\n    print(f\"{name}: {score:.4f}\")\n\nprint(\"\\n=== Stacking Classifier ===\\n\")\n\n# Stacking with Random Forest as meta-model\nstacking = StackingClassifier(\n    estimators=[\n        ('lr', LogisticRegression(max_iter=1000, random_state=42)),\n        ('dt', DecisionTreeClassifier(max_depth=5, random_state=42)),\n        ('rf', RandomForestClassifier(n_estimators=50, max_depth=5, random_state=42)),\n        ('gb', GradientBoostingClassifier(n_estimators=50, max_depth=3, random_state=42)),\n        ('knn', KNeighborsClassifier(n_neighbors=5))\n    ],\n    final_estimator=LogisticRegression(random_state=42),\n    cv=5\n)\nstacking.fit(X_train, y_train)\nstacking_score = stacking.score(X_test, y_test)\nprint(f\"Stacking (Logistic Regression meta): {stacking_score:.4f}\")\n\nprint(\"\\n=== Different Meta-Models ===\\n\")\n\n# Try different meta-models\nmeta_models = [\n    ('Logistic Regression', LogisticRegression(max_iter=1000, random_state=42)),\n    ('Random Forest', RandomForestClassifier(n_estimators=50, max_depth=5, random_state=42)),\n    ('Gradient Boosting', GradientBoostingClassifier(n_estimators=50, max_depth=3, random_state=42))\n]\n\nfor meta_name, meta_model in meta_models:\n    stacking = StackingClassifier(\n        estimators=[\n            ('lr', LogisticRegression(max_iter=1000, random_state=42)),\n            ('dt', DecisionTreeClassifier(max_depth=5, random_state=42)),\n            ('rf', RandomForestClassifier(n_estimators=50, max_depth=5, random_state=42)),\n            ('gb', GradientBoostingClassifier(n_estimators=50, max_depth=3, random_state=42)),\n            ('knn', KNeighborsClassifier(n_neighbors=5))\n        ],\n        final_estimator=meta_model,\n        cv=5\n    )\n    stacking.fit(X_train, y_train)\n    score = stacking.score(X_test, y_test)\n    print(f\"Meta={meta_name}: {score:.4f}\")\n\nprint(\"\\n=== Cross-Validation ===\\n\")\n\n# Cross-validation for stacking\nstacking_cv = cross_val_score(\n    StackingClassifier(\n        estimators=[\n            ('lr', LogisticRegression(max_iter=1000, random_state=42)),\n            ('rf', RandomForestClassifier(n_estimators=50, max_depth=5, random_state=42)),\n            ('gb', GradientBoostingClassifier(n_estimators=50, max_depth=3, random_state=42))\n        ],\n        final_estimator=LogisticRegression(random_state=42),\n        cv=5\n    ),\n    X_train, y_train, cv=5\n)\nprint(f\"Stacking 5-fold CV: {stacking_cv}\")\nprint(f\"Mean CV: {stacking_cv.mean():.4f}\")\n\nprint(\"\\n=== Stacking Best Practices ===\\n\")\n\nprint(\"\"\"\nBest practices:\n  1. ✅ Use diverse base models\n  2. ✅ Use cross-validation for training\n  3. ✅ Keep base models simple\n  4. ✅ Use a simple meta-model\n  5. ✅ Check for overfitting\n\nWhen to use Stacking:\n  ✅ When individual models perform differently\n  ✅ When you want to combine strengths\n  ✅ When you need the best possible performance\n\nWhen to avoid:\n  ❌ Small datasets\n  ❌ Need interpretability\n  ❌ Limited computation\n  ❌ Real-time predictions\n\"\"\")",
    "output": "(Stacking outputs displayed)\n\n=== Stacking and Blending ===\n\nStacking combines multiple models using a meta-model.\n\nHow it works:\n  1. Train base models on training data\n  2. Base models make predictions on validation data\n  3. Use these predictions to train meta-model\n  4. Final prediction from meta-model\n\nBlending is similar but uses a hold-out set instead of cross-validation.\n\nPros:\n  ✅ Can improve performance\n  ✅ Leverages different models\n  ✅ Flexible\n\nCons:\n  ❌ More complex\n  ❌ Slower\n  ❌ Can overfit\n  ❌ Less interpretable\n\n=== Creating Data ===\n\nTraining: 400, Test: 100\n\n=== Individual Models ===\n\nLogistic Regression: 0.8400\nDecision Tree: 0.8400\nRandom Forest: 0.8800\nGradient Boosting: 0.8900\nKNN: 0.8700\n\n=== Stacking Classifier ===\n\nStacking (Logistic Regression meta): 0.8900\n\n=== Different Meta-Models ===\n\nMeta=Logistic Regression: 0.8900\nMeta=Random Forest: 0.8900\nMeta=Gradient Boosting: 0.8900\n\n=== Cross-Validation ===\n\nStacking 5-fold CV: [0.8750 0.8875 0.8875 0.8750 0.8875]\nMean CV: 0.8825\n\n=== Stacking Best Practices ===\n\nBest practices:\n  1. ✅ Use diverse base models\n  2. ✅ Use cross-validation for training\n  3. ✅ Keep base models simple\n  4. ✅ Use a simple meta-model\n  5. ✅ Check for overfitting\n\nWhen to use Stacking:\n  ✅ When individual models perform differently\n  ✅ When you want to combine strengths\n  ✅ When you need the best possible performance\n\nWhen to avoid:\n  ❌ Small datasets\n  ❌ Need interpretability\n  ❌ Limited computation\n  ❌ Real-time predictions",
    "explanation": "🐍 **Stacking combines multiple models to improve performance.**\n\n**Implementation:**\n```python\nfrom sklearn.ensemble import StackingClassifier\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.ensemble import RandomForestClassifier\n\nstacking = StackingClassifier(\n    estimators=[\n        ('rf', RandomForestClassifier(n_estimators=50)),\n        ('gb', GradientBoostingClassifier(n_estimators=50))\n    ],\n    final_estimator=LogisticRegression(),\n    cv=5\n)\n```\n\n**Key parameters:**\n- `estimators`: List of (name, model) pairs\n- `final_estimator`: Meta-model\n- `cv`: Cross-validation folds\n- `stack_method`: 'predict', 'predict_proba'\n\n**Python-chan says:**\n\"Stacking is like having a team of experts with a manager. Each expert contributes.\"",
    "openingDialogue": "Stacking and Blending — combining models. 🏗️"
  },
  "phase2": {
    "brokenCode": "# Stacking with similar models\n\nfrom sklearn.ensemble import StackingClassifier\nfrom sklearn.tree import DecisionTreeClassifier\nfrom sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier\n\n# ❌ Bad: All similar models\nestimators = [\n    ('dt1', DecisionTreeClassifier()),\n    ('dt2', DecisionTreeClassifier()),\n    ('dt3', DecisionTreeClassifier())\n]\n\n# ✅ Good: Diverse models\nestimators = [\n    ('dt', DecisionTreeClassifier()),\n    ('rf', RandomForestClassifier()),\n    ('svm', SVC()),\n    ('knn', KNeighborsClassifier())\n]\n\nprint(\"✅ Use diverse models for stacking!\")\nprint(\"Similar models don't add much value.\")",
    "errorMessage": "✅ Use diverse models for stacking!\nSimilar models don't add much value.\n\n(No error — but stacking with similar models is wasteful!)",
    "explanation": "**Stacking with similar models is wasteful!**\n\n**What's wrong:**\n1. All models are Decision Trees\n2. They make similar errors\n3. Little benefit from combining\n\n**The fix:**\n```python\nfrom sklearn.ensemble import StackingClassifier\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.tree import DecisionTreeClassifier\nfrom sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier\nfrom sklearn.svm import SVC\nfrom sklearn.neighbors import KNeighborsClassifier\n\n# ✅ Good: Diverse models\nestimators = [\n    ('lr', LogisticRegression(max_iter=1000)),\n    ('dt', DecisionTreeClassifier(max_depth=5)),\n    ('rf', RandomForestClassifier(n_estimators=50)),\n    ('gb', GradientBoostingClassifier(n_estimators=50)),\n    ('svm', SVC(kernel='rbf', probability=True)),\n    ('knn', KNeighborsClassifier(n_neighbors=5))\n]\n\nstacking = StackingClassifier(\n    estimators=estimators,\n    final_estimator=LogisticRegression(),\n    cv=5\n)\n```\n\n**Python-chan says:**\n\"Stacking works best with diverse models. Different models = different strengths.\"",
    "openingDialogue": "Stacking with similar models — no benefit! ⚠️"
  },
  "xpReward": 10
}